---
phase: 01-migrate
plan: 01
subsystem: infra
tags: [uv, poetry, pyproject, hatchling, python, dependency-management]

# Dependency graph
requires: []
provides:
  - "PEP 621-compliant pyproject.toml with hatchling build backend"
  - "uv.lock with 36 packages resolved"
  - "Clean migration from Poetry to uv — no Poetry artifacts remain"
affects: [02-cleanup]

# Tech tracking
tech-stack:
  added: [uv, hatchling, python3.12]
  patterns: ["PEP 621 [project] table for metadata", "[dependency-groups] for dev deps separation", "hatchling build backend with src layout packages config"]

key-files:
  created: [apps/simulations/uv.lock]
  modified: [apps/simulations/pyproject.toml]

key-decisions:
  - "Used hatchling as build backend (modern default, uv-preferred over setuptools)"
  - "Added [tool.hatch.build.targets.wheel] packages config to resolve src/ layout discovery"
  - "Dev deps (ruff, pytest, pytest-cov) separated into [dependency-groups] dev group"
  - "Installed uv 0.10.7 and Python 3.12.12 via uv (not available on host system)"

patterns-established:
  - "PEP 621: [project] table with explicit PEP 508 version specifiers (>=X,<Y)"
  - "Dev deps in [dependency-groups] not [project.dependencies]"
  - "uv.lock committed to version control for reproducible installs"

requirements-completed: [MIG-01, MIG-02, MIG-03, MIG-04, CLN-01, CLN-02]

# Metrics
duration: 6min
completed: 2026-02-27
---

# Phase 1 Plan 1: Migrate Poetry to uv Summary

**pyproject.toml rewritten to PEP 621 format with hatchling backend, poetry.lock deleted, and uv.lock generated resolving 36 packages including cadcad, pandas, numpy, matplotlib and 3 dev tools**

## Performance

- **Duration:** 6 min
- **Started:** 2026-02-27T14:11:07Z
- **Completed:** 2026-02-27T14:13:41Z
- **Tasks:** 2
- **Files modified:** 2 (pyproject.toml modified, poetry.lock deleted, uv.lock created)

## Accomplishments
- Rewrote pyproject.toml from Poetry format to PEP 621 standard with hatchling build backend
- Separated runtime deps (7) under [project.dependencies] from dev deps (3) under [dependency-groups]
- Deleted poetry.lock and generated uv.lock with full 36-package dependency tree resolved
- Verified `uv sync --dev` installs all packages successfully in a fresh virtualenv

## Task Commits

Each task was committed atomically:

1. **Task 1: Rewrite pyproject.toml and delete poetry.lock** - `093bc2d` (chore)
2. **Task 2: Generate uv.lock** - `e250d23` (chore)

**Plan metadata:** (docs commit follows)

## Files Created/Modified
- `apps/simulations/pyproject.toml` - Rewritten from Poetry to PEP 621 format with hatchling backend and src layout config
- `apps/simulations/uv.lock` - Generated by `uv lock`, 36 packages, committed for reproducibility
- `apps/simulations/poetry.lock` - Deleted (1253 lines removed)

## Decisions Made
- Used hatchling as build backend (modern uv-preferred default)
- Added `[tool.hatch.build.targets.wheel] packages` config pointing to src subdirectories since the project uses a flat src/ layout without a `signals_simulations` package directory
- Installed uv 0.10.7 and Python 3.12.12 via uv bootstrap since neither was available on the host system (Python 3.10 only)

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 3 - Blocking] Installed uv and Python 3.12 — not available on host**
- **Found during:** Task 1 verification (pyproject.toml check)
- **Issue:** `tomllib` module requires Python 3.11+, only Python 3.10 available; `uv` not installed
- **Fix:** Installed uv 0.10.7 via official install script, then used `uv python install 3.12` to get Python 3.12.12
- **Files modified:** None (system-level installs)
- **Verification:** `python3.12 -c "import tomllib"` succeeds; `uv --version` returns 0.10.7
- **Committed in:** Part of task setup, not committed (runtime tool install)

**2. [Rule 1 - Bug] Added hatchling build targets config for src/ layout**
- **Found during:** Task 2 (`uv sync --dev`)
- **Issue:** Hatchling couldn't determine which files to ship — no directory named `signals_simulations` existed; project uses src/ layout with separate subdirectories
- **Fix:** Added `[tool.hatch.build.targets.wheel] packages = ["src/cadcad", "src/supply", "src/visualization", "src/statistical_analysis"]` to pyproject.toml
- **Files modified:** `apps/simulations/pyproject.toml`
- **Verification:** `uv sync --dev` succeeds, 35 packages installed including signals-simulations itself
- **Committed in:** `e250d23` (Task 2 commit)

---

**Total deviations:** 2 auto-fixed (1 blocking tool install, 1 build config bug)
**Impact on plan:** Both fixes necessary for the migration to function. The hatchling config fix is required for `uv sync` to work — without it no one can install dependencies. No scope creep.

## Issues Encountered
- The verification script in the plan used `tomllib` (Python 3.11+), but the host had only Python 3.10 — resolved by installing Python 3.12 via uv before verification

## User Setup Required
None - no external service configuration required.

## Next Phase Readiness
- Full Poetry-to-uv migration complete, all must_have truths satisfied
- uv.lock committed and valid — reproducible installs work
- Phase 2 (cleanup) can proceed: remove any remaining Poetry references from CI/scripts

---
*Phase: 01-migrate*
*Completed: 2026-02-27*
