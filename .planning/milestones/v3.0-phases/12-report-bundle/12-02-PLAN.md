---
phase: 12-report-bundle
plan: 02
type: execute
wave: 2
depends_on:
  - 12-01
files_modified:
  - apps/simulations/src/backtesting/report.py
  - apps/simulations/tests/test_report.py
autonomous: true
requirements:
  - REPT-05
  - REPT-06

must_haves:
  truths:
    - "Per-config detail plots are generated for best and worst N configurations by ranking metric"
    - "A multi-panel composite figure exists as a single saved PNG combining heatmaps and flip breakdown"
    - "generate_sweep_report produces composite.png in the output directory"
    - "generate_sweep_report produces detail/ plots for best and worst configs"
  artifacts:
    - path: "apps/simulations/src/backtesting/report.py"
      provides: "_plot_cell_detail, _build_composite, updated generate_sweep_report"
      min_lines: 200
    - path: "apps/simulations/tests/test_report.py"
      provides: "Tests for detail plots and composite figure"
      min_lines: 80
  key_links:
    - from: "apps/simulations/src/backtesting/report.py"
      to: "summary_df.nlargest/nsmallest"
      via: "Best/worst N config selection for detail plots"
      pattern: "nlargest|nsmallest"
    - from: "apps/simulations/src/backtesting/report.py"
      to: "Figure.subplot_mosaic"
      via: "Multi-panel composite layout"
      pattern: "subplot_mosaic"
---

<objective>
Add per-config detail plots (best/worst N configurations) and multi-panel composite figure to the report module, completing REPT-05 and REPT-06.

Purpose: Completes the report bundle with the remaining visualization types — detail bar charts for individual configs and a publication-ready composite figure that combines key insights into a single image.

Output: Updated `report.py` with detail plots and composite figure; updated `test_report.py` with new tests.
</objective>

<execution_context>
@/home/biscii/.claude/get-shit-done/workflows/execute-plan.md
@/home/biscii/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-report-bundle/12-RESEARCH.md
@.planning/phases/12-report-bundle/12-01-SUMMARY.md
@apps/simulations/src/backtesting/report.py
@apps/simulations/src/backtesting/plots.py

<interfaces>
<!-- From Plan 01 output (report.py) — executor reads the actual file -->

From src/backtesting/report.py (created in Plan 01):
```python
@dataclass(frozen=True)
class ReportResult:
    output_dir: str
    saved_paths: list[str]

def generate_sweep_report(sweep_result, *, best_n=3, worst_n=3, ranking_metric='flip_rate', row_axis='curve_type', col_axis='alpha') -> ReportResult:
    """Orchestrator — Plan 01 creates heatmaps + CSV/JSON. Plan 02 adds detail + composite."""

def _plot_metric_heatmap(summary_df, metric, row_axis, col_axis, cmap, title=None) -> Figure:
    """Already implemented in Plan 01."""

_METRIC_CMAPS: dict[str, str]  # metric name -> colormap string
```

From src/backtesting/plots.py:
```python
BLUE = '#2196F3'
ORANGE = '#FF9800'
# OO API pattern: Figure() + ax.bar(), no pyplot
```

summary_df columns: cell_id, curve_type, alpha, lock_profile_short, lock_profile_long, allocation_strategy, mc_dist_label, vote_timing_label, failed, flip_rate, gini_legacy, gini_signals, participation_rate, margin_shift_mean, margin_shift_std, enp_legacy_mean, enp_signals_mean, nakamoto_legacy_mean, nakamoto_signals_mean
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add _plot_cell_detail, _build_composite, and wire into generate_sweep_report</name>
  <files>apps/simulations/src/backtesting/report.py</files>
  <action>
Add two new private functions and update `generate_sweep_report()` to call them.

**_plot_cell_detail(row: pd.Series, title: str) -> Figure:**
- REPT-06: Bar chart showing all metric values for a single sweep cell.
- Extract metrics from the row: flip_rate, gini_legacy, gini_signals, participation_rate, enp_legacy_mean, enp_signals_mean, nakamoto_legacy_mean, nakamoto_signals_mean, margin_shift_mean.
- Replace NaN values with 0.0 for plotting (bar height).
- Create `Figure(figsize=(10, 5))`, add subplot.
- Use ORANGE color for bars (import from plots.py or define locally as `'#FF9800'`).
- Rotate x-axis labels 45 degrees.
- Title should identify the cell (e.g., "Best #1 — cell 7 (sqrt, alpha=0.5)").
- Return Figure.

**_build_composite(summary_df: pd.DataFrame, row_axis: str, col_axis: str) -> Figure:**
- REPT-05: Multi-panel composite figure.
- Use `Figure(figsize=(18, 12))` with `subplot_mosaic()`.
- Layout suggestion (2x2):
  ```python
  layout = [
      ['flip_heatmap', 'gini_heatmap'],
      ['enp_heatmap', 'margin_heatmap'],
  ]
  ```
- For each panel, render the appropriate metric heatmap directly into the axes (inline the imshow logic — do NOT call `_plot_metric_heatmap` since that creates its own Figure). Use `origin='lower'`, appropriate cmap from `_METRIC_CMAPS`, and cell annotations.
- Filter `df[df['failed'] == False]` before pivoting.
- Add `fig.suptitle('Sweep Report Summary', fontsize=16, fontweight='bold')`.
- Call `fig.tight_layout(rect=[0, 0, 1, 0.96])` to leave room for suptitle.
- Return Figure.

**Update generate_sweep_report():**
- After the existing heatmap generation loop, add:
  1. REPT-06: Per-config detail plots
     - Filter `valid_df = df[df['failed'] == False]`.
     - If valid_df is non-empty and has the ranking_metric column:
       - For `best_n`: use `valid_df.nlargest(best_n, ranking_metric)`. For each row, call `_plot_cell_detail(row, title)`. Save to `detail/best_{rank:02d}_cell_{cell_id}.png`.
       - For `worst_n`: use `valid_df.nsmallest(worst_n, ranking_metric)`. Save to `detail/worst_{rank:02d}_cell_{cell_id}.png`.
     - Save at dpi=300, bbox_inches='tight'.
  2. REPT-05: Composite figure
     - Call `_build_composite(df, row_axis, col_axis)`.
     - Save to `composite.png` at dpi=200, bbox_inches='tight'.
  - Append all new paths to `saved` list.

Handle edge cases:
- If `valid_df` has fewer rows than `best_n` or `worst_n`, just use what's available (nlargest/nsmallest handle this gracefully).
- If `summary_df` is empty, skip detail and composite generation.
  </action>
  <verify>
    <automated>cd /home/biscii/src/signals/apps/simulations && python -c "from backtesting.report import generate_sweep_report; print('import OK')"</automated>
  </verify>
  <done>report.py has _plot_cell_detail and _build_composite functions. generate_sweep_report creates detail plots for best/worst N configs and a composite.png.</done>
</task>

<task type="auto">
  <name>Task 2: Add tests for detail plots and composite figure</name>
  <files>apps/simulations/tests/test_report.py</files>
  <action>
Add tests to the existing `test_report.py` file.

**New tests:**

1. `test_detail_plots_created(tmp_path)` — call generate_sweep_report with best_n=2, worst_n=1. Assert `detail/best_01_cell_*.png` and `detail/worst_01_cell_*.png` files exist (use glob pattern).

2. `test_composite_figure_created(tmp_path)` — call generate_sweep_report. Assert `composite.png` exists in the output directory.

3. `test_plot_cell_detail_returns_figure(tmp_path)` — create a test pd.Series with metric columns, call `_plot_cell_detail(row, 'Test')`. Assert result is `isinstance(fig, Figure)`.

4. `test_build_composite_returns_figure(tmp_path)` — call `_build_composite` with test summary_df. Assert result is `isinstance(fig, Figure)`. Verify figure has multiple axes (at least 4 panels).

5. `test_detail_plot_handles_nan_metrics(tmp_path)` — create a row with some NaN metric values. Call `_plot_cell_detail`. Assert it returns a Figure without error (NaN → 0.0 for bar height).

6. `test_saved_paths_include_detail_and_composite(tmp_path)` — call generate_sweep_report. Assert `ReportResult.saved_paths` includes paths containing 'detail/' and 'composite.png'.

Reuse the `_make_sweep_result` fixture from Task 2 of Plan 01.
  </action>
  <verify>
    <automated>cd /home/biscii/src/signals/apps/simulations && python -m pytest tests/test_report.py -x -v</automated>
  </verify>
  <done>All test_report.py tests pass including new tests for detail plots and composite figure.</done>
</task>

</tasks>

<verification>
1. `python -m pytest tests/test_report.py -x -v` — all tests pass (both Plan 01 and Plan 02 tests)
2. `detail/` directory has best and worst config PNGs after generate_sweep_report
3. `composite.png` exists after generate_sweep_report
4. No pyplot usage in report.py
5. All existing tests still pass: `python -m pytest tests/ -x`
</verification>

<success_criteria>
- Per-config detail bar charts are generated for best N and worst N configurations
- Multi-panel composite figure combines key heatmaps into a single PNG
- generate_sweep_report returns ReportResult with all saved paths including detail/ and composite.png
- All tests pass (Plan 01 and Plan 02 tests)
- No pyplot state used — OO API only
</success_criteria>

<output>
After completion, create `.planning/phases/12-report-bundle/12-02-SUMMARY.md`
</output>
