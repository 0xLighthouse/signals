---
phase: 10.1-integration-wiring-fixes
verified: 2026-02-28T14:00:00Z
status: passed
score: 5/5 must-haves verified
re_verification: false
---

# Phase 10.1: Integration Wiring Fixes Verification Report

**Phase Goal:** Cross-phase integration issues in completed phases 8-10 are resolved — _nakamoto() follows the NaN-for-degenerate convention, mc_dist is reachable from the sweep grid and pipeline, and VoteTimingConfig is properly exported
**Verified:** 2026-02-28T14:00:00Z
**Status:** passed
**Re-verification:** No — initial verification

## Goal Achievement

### Observable Truths

| #   | Truth | Status | Evidence |
| --- | ----- | ------ | -------- |
| 1   | `_nakamoto()` returns `np.nan` (not `0`) when called with zero-weight arrays | VERIFIED | `metrics.py` line 65-66: `if len(weights) == 0 or weights.sum() == 0: return float(np.nan)`. Signature changed to `int \| float`. `test_nakamoto_nan_for_zero_weight` and `test_nakamoto_nan_for_empty_array` both pass. |
| 2   | `sweep.py` uses `np.nanmean` for all nakamoto metric aggregation | VERIFIED | `sweep.py` lines 372-373 use `float(np.nanmean(...))` for both `nakamoto_legacy_mean` and `nakamoto_signals_mean`. Pattern confirmed by grep. |
| 3   | `SweepConfig` accepts `mc_dists` axis and `_run_cell()` passes `mc_dist` to `generate_scenario()` | VERIFIED | `SweepConfig.mc_dists: list[AllocationDistribution] \| None = None` (line 64), `SweepCell.mc_dist: AllocationDistribution \| None = None` (line 85). `_enumerate_cells()` includes `mc_dist_values` in `itertools.product`. `_run_cell()` line 335: `kwargs['mc_dist'] = cell.mc_dist` when non-None. |
| 4   | `run_pipeline()` accepts and passes through `mc_dist` and `vote_timing` parameters | VERIFIED | `pipeline.py` `run_pipeline()` signature includes `mc_dist: AllocationDistribution \| None = None` and `vote_timing: VoteTimingConfig \| None = None`. Lines 259-262 inject both into `data_cfg` before calling `generate_scenario()`. `_GENERATE_SCENARIO_KEYS` frozenset includes both keys at line 211. |
| 5   | `VoteTimingConfig` is importable from `backtesting.data` | VERIFIED | `data/__init__.py` line 18: `VoteTimingConfig` in the `from backtesting.data.factory import (...)` block; line 51: `'VoteTimingConfig'` in `__all__`. `test_vote_timing_config_importable_from_data` passes. |

**Score:** 5/5 truths verified

### Required Artifacts

| Artifact | Expected | Status | Details |
| -------- | -------- | ------ | ------- |
| `apps/simulations/src/backtesting/metrics.py` | `_nakamoto()` with NaN-for-degenerate convention | VERIFIED | Contains `float(np.nan)` at degenerate-input guard; `NakamotoResult` annotations updated to `dict[str, int \| float]` |
| `apps/simulations/src/backtesting/sweep.py` | `SweepConfig.mc_dists`, `SweepCell.mc_dist`, `np.nanmean` aggregation | VERIFIED | All three patterns present; `mc_dist_label` column emitted in both `_run_cell()` success row and `_make_failed_row()` for schema alignment; `mc_dists` serialized as `repr()` strings in `config.json` export |
| `apps/simulations/src/backtesting/pipeline.py` | `mc_dist`/`vote_timing` in `_GENERATE_SCENARIO_KEYS` and `run_pipeline()` params | VERIFIED | Both keys in frozenset at line 211; both params in `run_pipeline()` signature; injection into `data_cfg` at lines 259-262 |
| `apps/simulations/src/backtesting/data/__init__.py` | `VoteTimingConfig` re-export | VERIFIED | Added to `from backtesting.data.factory import (...)` block and to `__all__` |

### Key Link Verification

| From | To | Via | Status | Details |
| ---- | -- | --- | ------ | ------- |
| `sweep.py _run_cell()` | `generate_scenario(mc_dist=)` | `kwargs['mc_dist'] = cell.mc_dist` | WIRED | Line 335: conditional injection when `cell.mc_dist is not None`; `_enumerate_cells()` cartesian product propagates `mc_dist` from `SweepConfig.mc_dists` to each `SweepCell` |
| `pipeline.py run_pipeline()` | `generate_scenario()` | `data_cfg['mc_dist'] = mc_dist` injection | WIRED | Lines 259-262 inject both `mc_dist` and `vote_timing` into `data_cfg` after the TOML filter; `events = generate_scenario(**data_cfg)` at line 263 |
| `data/__init__.py` | `factory.VoteTimingConfig` | re-export import | WIRED | `from backtesting.data.factory import (..., VoteTimingConfig, ...)` at lines 14-20; symbol in `__all__` at line 51 |

### Requirements Coverage

INT-01 through INT-04 are defined in `.planning/v3.0-MILESTONE-AUDIT.md` (not in `.planning/REQUIREMENTS.md`, which does not contain integration gap IDs). All four INT requirements are satisfied:

| Requirement | Source | Description | Status | Evidence |
| ----------- | ------ | ----------- | ------ | -------- |
| INT-01 | v3.0-MILESTONE-AUDIT.md | `mc_dist` not wired from sweep runner to `generate_scenario()` | SATISFIED | `SweepConfig.mc_dists` field added; `_enumerate_cells()` wires through cartesian; `_run_cell()` injects `kwargs['mc_dist']` |
| INT-02 | v3.0-MILESTONE-AUDIT.md | `_nakamoto()` returns 0 not `np.nan`; sweep uses `np.mean` not `np.nanmean` | SATISFIED | `_nakamoto()` returns `float(np.nan)` for degenerate inputs; both nakamoto aggregation lines use `np.nanmean` |
| INT-03 | v3.0-MILESTONE-AUDIT.md | `mc_dist`/`vote_timing` stripped by `pipeline._GENERATE_SCENARIO_KEYS` | SATISFIED | Both keys added to frozenset; `run_pipeline()` accepts them as explicit params and injects into `data_cfg` |
| INT-04 | v3.0-MILESTONE-AUDIT.md | `VoteTimingConfig` not re-exported from `data/__init__.py` | SATISFIED | Added to factory import block and `__all__` in `data/__init__.py` |

Note: REQUIREMENTS.md does not define INT-series IDs. These gap IDs are defined in the milestone audit document. No orphaned requirements found.

### Anti-Patterns Found

No blocking anti-patterns detected. The single match for "placeholder" in sweep.py is a legitimate docstring term for `_make_failed_row()` ("Dict with grid params and NaN metric placeholders"), not a stub or incomplete implementation.

| File | Line | Pattern | Severity | Impact |
| ---- | ---- | ------- | -------- | ------ |
| `sweep.py` | 265 | `"placeholder"` in docstring | Info | Docstring language describing NaN placeholders; not a stub |

### Human Verification Required

None. All integration points are verifiable programmatically through the test suite and static analysis. The 178-test suite (including 9 new INT-specific tests) fully covers the goal.

### Test Execution Summary

```
178 passed, 10 warnings in 19.23s
```

New tests added (all pass):
- `test_metrics.py::test_nakamoto_nan_for_zero_weight` — INT-02 zero-weight NaN
- `test_metrics.py::test_nakamoto_nan_for_empty_array` — INT-02 empty-array NaN
- `test_metrics.py::test_nakamoto_returns_int_for_valid_weights` — INT-02 regression guard
- `test_sweep.py::test_make_failed_row_has_nan_nakamoto` — schema consistency
- `test_sweep.py::test_enumerate_cells_with_mc_dists` — INT-01 mc_dists axis
- `test_sweep.py::test_enumerate_cells_mc_dists_none_backward_compat` — INT-01 backward compat
- `test_sweep.py::test_enumerate_cells_mc_dists_cartesian` — INT-01 cartesian product
- `test_pipeline.py::test_generate_scenario_keys_include_mc_dist_vote_timing` — INT-03 keys
- `test_pipeline.py::test_vote_timing_config_importable_from_data` — INT-04 re-export

Prior test count was 169; 9 new tests bring total to 178. Zero regressions.

### Commit Verification

Both task commits from the SUMMARY exist in git history:
- `4a9b88b` — `fix(10.1-01): Fix _nakamoto() NaN convention and sweep np.nanmean (INT-02)`
- `039329d` — `fix(10.1-01): Wire mc_dist through sweep/pipeline; re-export VoteTimingConfig (INT-01/03/04)`

---

_Verified: 2026-02-28T14:00:00Z_
_Verifier: Claude (gsd-verifier)_
