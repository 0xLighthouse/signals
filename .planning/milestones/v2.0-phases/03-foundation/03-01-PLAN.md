---
phase: 03-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/simulations/src/backtesting/__init__.py
  - apps/simulations/src/backtesting/data/__init__.py
  - apps/simulations/src/backtesting/data/schema.py
  - apps/simulations/src/backtesting/weighting/__init__.py
  - apps/simulations/src/backtesting/weighting/signals.py
  - apps/simulations/tests/test_schema.py
  - apps/simulations/tests/test_weighting.py
  - apps/simulations/pyproject.toml
autonomous: true
requirements: [DATA-01, DATA-06, WGHT-01, WGHT-02, WGHT-03, WGHT-04]

must_haves:
  truths:
    - "Pydantic v2 models exist for PROPOSAL_CREATED, VOTE_CAST, and PROPOSAL_FINALIZED with Governor Bravo field names"
    - "Pydantic rejects malformed events (bad support value, negative weight, end_block <= start_block) at construction time"
    - "EventType and VoteSupport are Python enums with correct values"
    - "GovernorDataLoader Protocol is defined and runtime-checkable"
    - "lock_curve() returns values in [floor, 1.0] for all four curve types (sqrt, linear, log, exp)"
    - "sqrt curve exhibits diminishing returns — delta decreases at 30/60/90/180 day intervals"
    - "compute_signals_weight() = stake * f(lock_duration) with no cadCAD import"
    - "compute_legacy_weight(stake) == stake for any positive value"
  artifacts:
    - path: "apps/simulations/src/backtesting/data/schema.py"
      provides: "Pydantic v2 event models, enums, GovernorDataLoader Protocol"
      exports: ["EventType", "VoteSupport", "ProposalCreatedEvent", "VoteCastEvent", "ProposalFinalizedEvent", "GovernorEvent", "GovernorDataLoader"]
    - path: "apps/simulations/src/backtesting/weighting/signals.py"
      provides: "Pure weighting functions — no cadCAD import"
      exports: ["lock_curve", "compute_signals_weight", "compute_legacy_weight", "CurveType"]
    - path: "apps/simulations/tests/test_schema.py"
      provides: "Tests for DATA-01, DATA-06"
    - path: "apps/simulations/tests/test_weighting.py"
      provides: "Tests for WGHT-01 through WGHT-04"
  key_links:
    - from: "apps/simulations/src/backtesting/weighting/signals.py"
      to: "numpy"
      via: "np.sqrt, np.log1p, np.exp for curve computations"
      pattern: "import numpy as np"
    - from: "apps/simulations/src/backtesting/data/schema.py"
      to: "pydantic"
      via: "BaseModel, field_validator, model_validator"
      pattern: "from pydantic import BaseModel"
---

<objective>
Create the Governor-compatible event schema (Pydantic v2 models, enums, Protocol) and pure weighting functions (lock curve, signals weight, legacy weight). These are the foundational contracts that all subsequent code depends on.

Purpose: Establish the validated data structures and scientific weight computation (W = stake * f(lock_duration)) independently of cadCAD, enabling isolated unit testing of the core claim.

Output: `backtesting/data/schema.py`, `backtesting/weighting/signals.py`, package init files, and full test coverage for both modules.
</objective>

<execution_context>
@/home/biscii/.claude/get-shit-done/workflows/execute-plan.md
@/home/biscii/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-foundation/03-CONTEXT.md
@.planning/phases/03-foundation/03-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Package structure, schema, and weighting modules</name>
  <files>
    apps/simulations/src/backtesting/__init__.py
    apps/simulations/src/backtesting/data/__init__.py
    apps/simulations/src/backtesting/data/schema.py
    apps/simulations/src/backtesting/weighting/__init__.py
    apps/simulations/src/backtesting/weighting/signals.py
    apps/simulations/pyproject.toml
  </files>
  <action>
    1. Create package directories and __init__.py files:
       - `src/backtesting/__init__.py` — empty or with version
       - `src/backtesting/data/__init__.py` — re-export key types from schema
       - `src/backtesting/weighting/__init__.py` — re-export key functions from signals

    2. Create `src/backtesting/data/schema.py` following the RESEARCH.md Pattern 1 exactly:
       - `EventType(str, Enum)` with PROPOSAL_CREATED, VOTE_CAST, PROPOSAL_FINALIZED
       - `VoteSupport(str, Enum)` with FOR, AGAINST, ABSTAIN (Governor Bravo tri-state per user decision)
       - `ProposalCreatedEvent(BaseModel)` with: event_type (Literal discriminator), block_number (int), proposal_id (str), proposer (str), start_block (int), end_block (int), description (str, default ''), quorum (int, default 0). Validators: block_numbers_non_negative, end_after_start.
       - `VoteCastEvent(BaseModel)` with: event_type (Literal), block_number (int), proposal_id (str), voter (str), support (VoteSupport), weight (float), lock_duration_days (float, default 0.0), unix_timestamp (Optional[int], default None). Validators: weight_non_negative, lock_duration_non_negative.
       - `ProposalFinalizedEvent(BaseModel)` with: event_type (Literal), block_number (int), proposal_id (str), passed (bool), unix_timestamp (Optional[int], default None).
       - `GovernorEvent = ProposalCreatedEvent | VoteCastEvent | ProposalFinalizedEvent`
       - `@runtime_checkable GovernorDataLoader(Protocol)` with `def load(self) -> pd.DataFrame`
       - Use Pydantic v2 decorators (`@field_validator`, `@model_validator(mode='after')`) — NOT v1 `@validator`.

    3. Create `src/backtesting/weighting/signals.py` following RESEARCH.md Pattern 2 exactly:
       - `CurveType = Literal['sqrt', 'linear', 'log', 'exp']`
       - `lock_curve(lock_duration_days, curve_type='sqrt', l_max_days=365.0, floor=0.1) -> float` — returns value in [floor, 1.0]. Caps at l_max. Floor at L<=0. Four curve implementations: sqrt, linear, log (np.log1p), exp (alpha=0.5/30 per day, normalized).
       - `compute_signals_weight(stake, lock_duration_days, ...) -> float` — returns stake * lock_curve(...)
       - `compute_legacy_weight(stake) -> float` — returns float(stake)
       - NO cadCAD import anywhere in this file. NO pandas. Pure functions only.

    4. Update `apps/simulations/pyproject.toml` [tool.hatch.build.targets.wheel] packages to include `"src/backtesting"`.
  </action>
  <verify>
    cd apps/simulations && uv run python -c "
from backtesting.data.schema import EventType, VoteSupport, ProposalCreatedEvent, VoteCastEvent, ProposalFinalizedEvent, GovernorDataLoader
from backtesting.weighting.signals import lock_curve, compute_signals_weight, compute_legacy_weight
print('EventType values:', [e.value for e in EventType])
print('VoteSupport values:', [e.value for e in VoteSupport])
print('lock_curve(0):', lock_curve(0))
print('lock_curve(365):', lock_curve(365))
print('signals_weight(1000, 90):', compute_signals_weight(1000, 90))
print('legacy_weight(1000):', compute_legacy_weight(1000))
print('All imports OK')
"
  </verify>
  <done>
    - All 3 Pydantic event models importable with correct fields and validators
    - EventType and VoteSupport enums have expected values
    - GovernorDataLoader Protocol is runtime-checkable
    - lock_curve returns floor (0.1) at L=0 and 1.0 at L=365
    - All four curve types (sqrt, linear, log, exp) produce values in [0.1, 1.0]
    - compute_signals_weight = stake * lock_curve
    - compute_legacy_weight = identity
    - backtesting package in hatch build targets
  </done>
</task>

<task type="auto">
  <name>Task 2: Test suite for schema and weighting</name>
  <files>
    apps/simulations/tests/test_schema.py
    apps/simulations/tests/test_weighting.py
  </files>
  <action>
    1. Create `tests/test_schema.py` covering DATA-01 and DATA-06:
       - `test_event_models_exist`: Import all 3 event types, verify they have the expected fields (use model_fields). Verify GovernorEvent union type exists.
       - `test_enum_values`: EventType has PROPOSAL_CREATED, VOTE_CAST, PROPOSAL_FINALIZED. VoteSupport has FOR, AGAINST, ABSTAIN.
       - `test_proposal_created_valid`: Construct a valid ProposalCreatedEvent, verify fields.
       - `test_vote_cast_valid`: Construct a valid VoteCastEvent with all support types.
       - `test_proposal_finalized_valid`: Construct valid event.
       - `test_validation_rejects_bad_events`: Use pytest.raises(ValidationError) for:
         (a) VoteCastEvent with support='INVALID'
         (b) VoteCastEvent with weight=-100
         (c) ProposalCreatedEvent with end_block <= start_block
         (d) ProposalCreatedEvent with negative block_number
         (e) VoteCastEvent with negative lock_duration_days
       - `test_default_values`: quorum defaults to 0, lock_duration_days defaults to 0.0, description defaults to ''.
       - `test_protocol_is_runtime_checkable`: isinstance check on a dummy class with load() -> DataFrame method.
       - `test_model_dump_produces_dict`: Verify .model_dump() on each event type produces a plain dict (important for cadCAD M param compatibility).

    2. Create `tests/test_weighting.py` covering WGHT-01 through WGHT-04:
       - `test_no_cadcad_import`: inspect.getsource(signals module), assert 'cadcad' not in source (case-insensitive).
       - `test_all_curve_types`: For each of sqrt, linear, log, exp: lock_curve(0) == floor (0.1), lock_curve(365) ~= 1.0, lock_curve(180) is in (floor, 1.0).
       - `test_floor_at_zero`: lock_curve(0) == 0.1 for all curve types. lock_curve(-5) == 0.1.
       - `test_cap_at_lmax`: lock_curve(500, l_max_days=365) == lock_curve(365, l_max_days=365) for all curve types.
       - `test_diminishing_returns`: For sqrt curve: compute deltas at 30, 60, 90, 180 days. Assert each subsequent delta is smaller than the previous (WGHT-03).
       - `test_signals_weight_formula`: compute_signals_weight(1000, 90) == 1000 * lock_curve(90). Test with multiple curve types.
       - `test_signals_weight_leq_stake`: For various lock durations, assert compute_signals_weight(stake, L) <= stake.
       - `test_legacy_weight_identity`: For stakes [0, 1, 100, 1e6, 0.001], assert compute_legacy_weight(s) == s.
       - `test_custom_floor`: lock_curve(0, floor=0.2) == 0.2. lock_curve(365, floor=0.2) == 1.0.
       - `test_custom_lmax`: lock_curve(180, l_max_days=180) ~= 1.0.
  </action>
  <verify>cd apps/simulations && uv run pytest tests/test_schema.py tests/test_weighting.py -x -q</verify>
  <done>
    - All schema tests pass: event models exist with correct fields, enums have correct values, Pydantic rejects all 5 malformed event cases, Protocol is runtime-checkable
    - All weighting tests pass: no cadCAD import, all 4 curve types produce correct ranges, diminishing returns confirmed for sqrt, legacy weight is identity, signals weight <= stake
  </done>
</task>

</tasks>

<verification>
cd apps/simulations && uv run pytest tests/test_schema.py tests/test_weighting.py -v
</verification>

<success_criteria>
- All Pydantic v2 event models (ProposalCreatedEvent, VoteCastEvent, ProposalFinalizedEvent) importable and validating
- Pydantic rejects malformed events at construction time (DATA-06)
- GovernorDataLoader Protocol defined and runtime-checkable
- lock_curve() supports all 4 curve types with correct floor/cap behavior (WGHT-02)
- sqrt curve shows diminishing returns (WGHT-03)
- compute_signals_weight = stake * f(lock_duration), no cadCAD import (WGHT-01)
- compute_legacy_weight = identity (WGHT-04)
- All tests green
</success_criteria>

<output>
After completion, create `.planning/phases/03-foundation/03-01-SUMMARY.md`
</output>
